---
import { FaApple, FaLinux, FaWindows } from 'react-icons/fa6';
import { LuDownload } from 'react-icons/lu';
import type { ReleaseAsset } from '../data/releases';

interface Props {
  asset: ReleaseAsset;
  version: string;
}

const { asset, version } = Astro.props;

const osIcons = {
  linux: FaLinux,
  macos: FaApple,
  windows: FaWindows,
} as const;

const Icon = osIcons[asset.icon];
---

<a
  href={asset.href}
  class="group flex min-w-0 flex-1 basis-76 items-center justify-between gap-4 rounded-2xl border border-white/10 bg-black/30 px-4 py-3.5 transition hover:border-emerald-400/30 hover:bg-zinc-900/80 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-300"
  aria-label={`Download ${version} ${asset.target} for ${asset.label} ${asset.arch}`}
>
  <div class="flex min-w-0 items-center gap-3">
    <div class="rounded-xl border border-emerald-500/20 bg-emerald-500/10 p-2 text-emerald-300">
      <Icon className="size-4" aria-hidden="true" />
    </div>
    <div class="min-w-0">
      <div class="flex flex-wrap items-center gap-x-2 gap-y-1">
        <span class="text-sm font-semibold text-zinc-100">{asset.label}</span>
        <span class="text-xs uppercase tracking-[0.16em] text-zinc-500">{asset.target}</span>
      </div>
      <p class="truncate text-sm text-zinc-400">
        {asset.arch} <span aria-hidden="true">·</span> {asset.format}
      </p>
    </div>
  </div>

  <span class="inline-flex items-center gap-2 text-sm font-medium text-emerald-300">
    Download
    <LuDownload className="size-4 transition group-hover:translate-y-px" aria-hidden="true" />
  </span>
</a>
